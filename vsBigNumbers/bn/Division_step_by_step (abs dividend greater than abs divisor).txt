/*
56402.5 / 0.05

ответ в десятичном формате:
1128050.0

ответ в научном формате:
1,12805 e+6
*/

/*
0-А. Подготавливаем оба числа:
разделить
56402.5 / 0.05

это то же, что и:
5640250 / 5

BigInt dividendInt = 5640250;
BigInt  divisorInt = 5;

BigInt quotientInt;
quotientInt.clear_number();
*/

/* 

BigInt curSubtotal('0');


1. 	BigFloat::calc_subtotal(BigInt prevSubtotal, BigInt dividendInt, BigInt divisorInt, size_t indexOfCurDigitOfDividend)

	BigInt prevSubtotal;
	BigInt incompleteDividend;
	incompleteDividend.clear_number();
	
	BigInt fitMultiplier('1');
	BigInt curMultiplier('2');

Первый виток цикла
	prevSubtotal = curSubtotal;
	
	incompleteDividend.push_back_elem(prevSubtotal.elem_value_as_char(0)); // incompleteDividend = 0
	incompleteDividend.push_back_elem(dividendInt.elem_value_as_char(indexOfCurDigitOfDividend++)); // incompleteDividend = 05
	incompleteDividend.pop_front_extra_zeros(); // incompleteDividend = 5
	
	while (incompleteDivivdend <= divisorInt)
	{
		incompleteDivivdend.push_back_elem(dividendInt.elem_value_as_char(indexOfCurDigitOfDividend++));
	}
	
	while (incompleteDivivdend <= divisorInt)
*/
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	